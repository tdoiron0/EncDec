[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jap2eng"
version = "0.1.0"

[tool.setuptools]
py-modules = ["utils"]

[tool.setuptools.packages.find]
where = ["."]