[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "encdec"
version = "0.1.0"

[tool.setuptools]
py-modules = ["utils", "index", "constants"]

[tool.setuptools.packages.find]
where = ["."]